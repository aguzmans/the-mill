export default {
  plugins: {
    // Tailwind v4's PostCSS plugin. It handles imports and vendor prefixing
    // internally (Lightning CSS), so autoprefixer is no longer needed.
    "@tailwindcss/postcss": {},
  },
};
